---
import { cn } from "@lib/cn.ts";

interface Props {
    as?: keyof HTMLElementTagNameMap;
    class?: string;
}

const {
    as: Tag = "div",
    class: className,
} = Astro.props;
---

<Tag class={cn("mx-auto w-full max-w-7xl px-6 lg:px-8", className)}>
    <slot />
</Tag>