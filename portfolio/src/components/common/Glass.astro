---
import { cn } from "@lib/cn.ts";

interface Props {
    variant?: "default" | "navbar" | "hero";
    class?: string;
}

const {
    variant = "default",
    class: className,
} = Astro.props;
---

<div
        class={cn("glass", className)}
        data-variant={variant}
>
    <slot />
</div>